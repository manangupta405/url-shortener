{
  "server": {
    "port": "{{SERVER_PORT}}"
  },
  "database": {
    "host": "{{DATABASE_HOST}}",
    "port": "{{DATABASE_PORT}}",
    "dbname": "{{DATABASE_NAME}}",
    "user": "{{DATABASE_USER}}",
    "password" : "{{DATABASE_PASSWORD}}"
  }
}
